# =============================================================================
# AI Tool Sync - Publish Workflow
# =============================================================================
# Publishes to npm when a release is created
# Supports both stable and pre-release versions

name: Publish

on:
  release:
    types: [published]

jobs:
  publish-npm:
    name: Publish to npm
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
      
      - name: Run tests
        run: npm test
      
      - name: Get release tag
        id: release
        run: |
          TAG="${GITHUB_REF#refs/tags/v}"
          if [[ "$TAG" == *"-"* ]]; then
            echo "NPM_TAG=next" >> $GITHUB_OUTPUT
          else
            echo "NPM_TAG=latest" >> $GITHUB_OUTPUT
          fi
      
      - name: Publish to npm
        run: npm publish --access public --tag ${{ steps.release.outputs.NPM_TAG }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  # ===========================================================================
  # Optional: Build standalone binaries for multiple platforms
  # ===========================================================================
  # build-binaries:
  #   name: Build Standalone Binaries
  #   runs-on: ${{ matrix.os }}
  #   strategy:
  #     matrix:
  #       include:
  #         - os: ubuntu-latest
  #           target: linux-x64
  #         - os: macos-latest
  #           target: darwin-x64
  #         - os: macos-latest
  #           target: darwin-arm64
  #         - os: windows-latest
  #           target: win-x64
  #   
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #     
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '22'
  #         cache: 'npm'
  #     
  #     - name: Install dependencies
  #       run: npm ci
  #     
  #     - name: Build
  #       run: npm run build
  #     
  #     - name: Package binary
  #       run: npx pkg . --target node22-${{ matrix.target }} --output ai-sync-${{ matrix.target }}
  #     
  #     - name: Upload binary to release
  #       uses: softprops/action-gh-release@v2
  #       with:
  #         files: ai-sync-${{ matrix.target }}*
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

